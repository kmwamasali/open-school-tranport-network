# Backend Applications

Each folder is a Django app boundary. Keep domain models, serializers, selectors, services, permissions, URLs, and tests inside the owning module.

Business rules should live in service modules, not directly in views.
